import { SimpleValidatorDirective } from './simple-validator.directive';

describe('SimpleValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
