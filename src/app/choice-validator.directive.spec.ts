import { ChoiceValidatorDirective } from './choice-validator.directive';

describe('ChoiceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChoiceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
